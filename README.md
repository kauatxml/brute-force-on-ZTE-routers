# brute-force-on-ZTE-routers
Python script for authorized authentication testing in a controlled environment. It creates an HTTP session, retrieves login tokens, submits credentials from a wordlist, detects successful logins, and refreshes session tokens to maintain reliable testing. For educational use only.
